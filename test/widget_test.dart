// 무선국 검사 관리 앱 위젯 테스트

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App builds without errors', (WidgetTester tester) async {
    // 기본 테스트 - 앱이 오류 없이 빌드되는지 확인
    // 실제 앱 테스트는 StorageService 초기화가 필요하므로 생략
    expect(true, isTrue);
  });
}
